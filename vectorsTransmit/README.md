# Vectors Transmit

This is to be run on a phone/other device that has odometry data. Basically, it's an HTML file that just needs to run, and it will transmit odometry data from the device to Vectors (the one made in Godot).